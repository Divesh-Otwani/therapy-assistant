<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="haverford.therapy_assistant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Homepage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.exercise.Exercises"
            android:label="@string/title_activity_exercises" />
        <activity
            android:name=".activity.resource.Resources"
            android:label="@string/title_activity_resources"></activity>
        <activity
            android:name=".activity.resource.CBTResources"
            android:label="@string/title_activity_cbt_resources"></activity>
    </application>

</manifest>